input {
  beats {
    port => 5044
  }
  tcp {
    port => 50000
  }
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    user => "elastic"
    password => "MiPasswordSeguro123"
    index => "logstash-%{+YYYY.MM.dd}"
  }
}